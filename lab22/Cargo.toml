[package]
name = "lab22"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", features = ["json", "rustls-tls"] } # Используем async
scraper = "0.17"
thiserror = "1.0"
url = "2.5" 
tokio = { version = "1", features = ["full"] } # Для асинхронного запуска
